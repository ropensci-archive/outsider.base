# TODO: allow servers

# server_connect <- function(host, keyfile = NULL, passwd = getPass::getPass(),
#                            verbose = FALSE) {
#   # TODO: askpass or getPass?
#   session <- ssh::ssh_connect(host = host, keyfile = keyfile, passwd = passwd,
#                               verbose = verbose)
#   # set in options()
#   # create working dir
#   invisible(TRUE)
# }
# 
# server_disconnect <- function() {
#   
# }
# 
# is_server_connected <- function() {
#   # does session exist in options?
# }
